"""
Модуль для настройки тестовых сценариев боя.
Используется исключительно при config.debug_mode = True.
Не содержит игровой логики, только данные для отладки.
"""
from src.objects.enemies.husk import Husk


def get_test_enemies() -> list:
    """
    Возвращает список врагов для тестового боя.
    Координаты подобраны для удобной проверки механик на тестовой карте.
    """
    enemies = []

    # Спавним хасков в стратегических точках (дальше, ближе, сбоку)
    enemies.append(Husk(x=700, y=200))

    return enemies